# Helium browser launch flags
# Require user interaction before media can autoplay.
--autoplay-policy=user-gesture-required

# Ignore keyboard hardware media keys.
--disable-features=HardwareMediaKeyHandling
